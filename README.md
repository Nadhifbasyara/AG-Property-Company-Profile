# AG Property

Landing page company profile untuk AG Property, dibuat dengan tampilan bersih, modern, dan fokus ke identitas properti di Sleman.

> Hunian berkualitas, investasi tepat, dan informasi perusahaan yang rapi dalam satu halaman.

## Preview

Halaman ini berisi:

- Hero section dengan visual properti yang kuat
- Layanan unggulan dengan ikon dari folder `assets`
- Section About, Visi, dan Misi
- Alasan memilih AG Property
- Kontak perusahaan dan peta lokasi Google Maps
- Footer dengan navigasi tambahan

## Tech Stack

- HTML5
- CSS3
- Google Fonts
- Google Maps embed

## Screenshot

Kalau kamu ingin menambahkan screenshot ke README, tempatkan file gambar di folder `assets/` lalu tambahkan bagian berikut:

```md
![Preview AG Property](assets/preview.png)
```

## Getting Started

### 1. Buka secara langsung

Buka file `index.html` di browser untuk melihat hasilnya.

### 2. Jalankan via local server

Kalau ingin lebih nyaman saat testing, jalankan server lokal:

```bash
python -m http.server 8000
```

Lalu buka:

```text
http://localhost:8000
```
<<<<<<< HEAD
=======

## Project Structure

```text
ag-property/
├── assets/
├── index.html
├── styles.css
└── README.md
```

## Deployment

### Update dari sini

Kalau kamu edit file di workspace ini, alurnya sederhana:

```bash
git add README.md
git commit -m "Update README"
git push
```

Kalau sekalian ada file lain yang diubah, pakai:

```bash
git add .
git commit -m "Update project files"
git push
```

### GitHub Pages

Kalau repository sudah ada di GitHub, aktifkan dari:

`Settings` > `Pages` > `Branch: main` > `/(root)`

### Push awal ke repo baru

Kalau kamu belum pernah menghubungkan repo ini ke GitHub, pakai:

```bash
git init
git add .
git commit -m "Initial company profile site"
git branch -M main
git remote add origin <URL_REPO_GITHUB>
git push -u origin main
```

## Catatan

- Ikon layanan menggunakan file gambar dari folder `assets`
- Kontak dan alamat sudah disesuaikan untuk AG Property
- Layout dibuat responsif untuk desktop dan mobile

## Next Improvements

Kalau mau dibuat lebih menarik lagi, langkah berikutnya yang paling berguna adalah:

- menambahkan screenshot nyata ke README
- menambahkan badge GitHub Actions atau GitHub Pages
- menambahkan custom domain lewat `CNAME`
- membuat versi bilingual Indonesia dan English
>>>>>>> f1afcc1 (Update README)
